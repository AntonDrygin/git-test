# git-test

Минимальный тестовый Python-проект для запуска в Codex.

## Structure
- src/main.py
- tests/test_main.py
- requirements.txt

## Goal
Используется для первого подключения проекта в Codex и проверки базового workflow:
read → plan → edit → review → commit.